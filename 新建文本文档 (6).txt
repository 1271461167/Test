show databases;
create database itcast;
use itcast;
show tables;
create table tb_user(id int, name varchar(50),age int,sex varchar(1));
insert into tb_user(id,name,age,sex)values(001,'张三',23,'男');
select* from user;
alter table tb_user change sex gender varchar(1);
show variables like '%ssl%';
use mysql;
select host,user,plugin from user;
ALTER USER 'root'@'%' IDENTIFIED WITH mysql_native_password BY '123456';
use mysql;
create table user(id int auto_increment primary key, name varchar(50),age int,gender int,avatar varchar(100),permission varchar(50),password varchar(50));
insert into user(name,age,gender,avatar,permission,password)values('admin',23,1,'../Assets/Images/admin.png','admin','123456');
insert into user(name,age,gender,avatar,permission,password)values('operater',22,2,'../Assets/Images/operater.png','operater','484987');
drop table user;

use itcast;
show tables;
create table course(id int auto_increment primary key, course_name varchar(50),ispublished int);
insert into course(course_name,ispublished)values('C#',1);
insert into course(course_name,ispublished)values('C++',0);
create table plantforms(plantname varchar(50) primary key,isvalid int);
insert into plantforms(plantname,isvalid)values('Bilibili',1);
insert into plantforms(plantname,isvalid)values('Huya',1);
insert into plantforms(plantname,isvalid)values('Weibo',1);
select * from plantforms;
select * from course;

///////////////////////////20240226////////////////////////////
show databases;
create database student;
use student;
create table user(name varchar(50), password varchar(100), permission varchar(50));
create table info(id int auto_increment primary key, name varchar(50),age int);
insert into user(name,password,permission) values("admin","123456","admin");
select * from user;
insert into info(name,age)values("zhangsan",13);
insert into info(name,age)values("lll",15);
insert into user(name,password,permission) values("operater","123456","operater");

select * from user where name='1' or '1'='1'and password ='1' or '1'='1';
select * from user where name='1' or '1'='1'and password ='1' or '1'='1';
select * from user where name='1' or '1'='1'and password ='1' or '1'='1';
create database itcast;
use itcast;
create table tb_user(id int, name varchar(50),age int,sex varchar(1));
drop table tb_user;
create table user(id int auto_increment primary key, name varchar(50),age int,gender int,avatar varchar(100),permission varchar(50),password varchar(50));
insert into user(name,age,gender,avatar,permission,password)values('admin',23,1,'../Assets/Images/admin.png','admin','123456');
insert into user(name,age,gender,avatar,permission,password)values('operater',22,2,'../Assets/Images/operater.png','operater','484987');
show tables;
select* from user;

use itcast;
show tables;
create table course(course_id varchar(50), course_name varchar(50),ispublished int);
insert into course(course_id,course_name,ispublished)values('C001','C#',1);
insert into course(course_id,course_name,ispublished)values('C002','C++',0);
create table plantforms(plant_id varchar(50),plant_name varchar(50),isvalid int);
insert into plantforms(plant_id,plant_name,isvalid)values('PF001','Bilibili',1);
insert into plantforms(plant_id,plant_name,isvalid)values('PF002','Huya',1);
insert into plantforms(plant_id,plant_name,isvalid)values('PF003','Weibo',1);
create table record(course_id varchar(50),plant_id varchar(50),play_count int,isgrowing int,growrate int);
insert into record(course_id,plant_id,play_count,isgrowing,growrate)values('C001','PF001',161,0,-75);
insert into record(course_id,plant_id,play_count,isgrowing,growrate)values('C001','PF002',283,1,4);
insert into record(course_id,plant_id,play_count,isgrowing,growrate)values('C001','PF003',902,0,-50);
insert into record(course_id,plant_id,play_count,isgrowing,growrate)values('C002','PF001',161,0,-75);
insert into record(course_id,plant_id,play_count,isgrowing,growrate)values('C002','PF002',283,1,4);
insert into record(course_id,plant_id,play_count,isgrowing,growrate)values('C002','PF003',902,0,-50);
select * from plantforms;
select * from course;
select * from record;
drop table course;
delete from record where growrate=4;
show variables like 'SQL_SAFE_UPDATES';
SET SQL_SAFE_UPDATES = 0;
select a.course_id,a.course_name,c.plant_name,b.play_count,b.isgrowing,b.growrate from course a left join record b on a.course_id=b.course_id left join plantforms c on b.plant_id=c.plant_id order by a.course_id,c.plant_id;